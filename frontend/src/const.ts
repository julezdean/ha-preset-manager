/** Names the card is known by. Changing either breaks existing dashboards. */

/** The custom element, and therefore `type: custom:preset-manager-card`. */
export const CARD_TYPE = "preset-manager-card";

/** Filled in by the build, so the console line says which bundle is loaded. */
export const CARD_VERSION = "__CARD_VERSION__";
